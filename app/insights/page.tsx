export default function InsightsPage() {
  // This route has been deprecated. Keeping a minimal component to avoid build errors
  // if some external link still points here. Consider deleting the directory entirely
  // once all references are removed.
  return null;
}
